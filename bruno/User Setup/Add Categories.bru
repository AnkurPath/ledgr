meta {
  name: Add Categories
  type: http
  seq: 8
}

post {
  url: {{base_url}}/users/setup/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "kind": "Expense",
    "name": "Personal"
  }
}
