meta {
  name: Add Account
  type: http
  seq: 5
}

post {
  url: {{base_url}}/users/{{user_id}}/setup/accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "HDFC Bank",
    "account_type": "Salary",
    "opening_balance": 10
  }
}
