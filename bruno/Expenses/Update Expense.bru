meta {
  name: Update Expense
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/expenses/{{expense_id}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "amount": "5.25",
    "notes": "Patched amount"
  }
}
